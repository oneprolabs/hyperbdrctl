package i18n

var ossZH = map[string]string{
	"cmd.object_storages.short":            "对象存储查询",
	"cmd.object_storages.long":             "列出对象存储、查看详情、发现 Bucket，或按已验证请求体创建对象存储。",
	"cmd.object_storages.examples":         "./hyperbdrctl object-storages list\n./hyperbdrctl object-storages buckets --auth-url oss-cn-beijing.aliyuncs.com --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk>\n./hyperbdrctl object-storages create --display-name aliyun-beijing --auth-url oss-cn-beijing.aliyuncs.com --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk> --bucket-mode existing --bucket-name <bucket>",
	"cmd.object_storages.notes":            "- `list` 和 `detail` 为只读。\n- `buckets` 用于校验凭证并列出现有 Bucket 候选。\n- `create` 会发起写操作。",
	"cmd.object_storages.list.short":       "列出对象存储",
	"cmd.object_storages.list.long":        "查询存储目标，默认 type=objectstorage。",
	"cmd.object_storages.list.examples":    "./hyperbdrctl object-storages list --page 1 --page-size 100",
	"cmd.object_storages.list.notes":       "- 未识别参数会透传为查询参数。\n- 如果需要引导式说明，请使用 boot-config-wizard storages。",
	"cmd.object_storages.detail.short":     "查看对象存储详情",
	"cmd.object_storages.detail.long":      "根据 ID 读取单个对象存储详情对象。",
	"cmd.object_storages.detail.examples":  "./hyperbdrctl object-storages detail --id <storage_id>",
	"cmd.object_storages.detail.notes":     "- 未识别参数会透传为查询参数。\n- 如需完整嵌套存储元数据，请使用 JSON 输出。",
	"cmd.object_storages.buckets.short":    "列出对象存储 Bucket",
	"cmd.object_storages.buckets.long":     "调用 UI 鉴权页使用的已验证 Bucket 发现接口。",
	"cmd.object_storages.buckets.examples": "./hyperbdrctl object-storages buckets --auth-url oss-cn-beijing.aliyuncs.com --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk>\n./hyperbdrctl object-storages buckets --auth-url oss-cn-beijing.aliyuncs.com --access-key-id <ak> --access-key-secret <sk> --bucket-lookup dns",
	"cmd.object_storages.buckets.notes":    "- 只读辅助命令。\n- `bucket-lookup=dns` 对应 UI 的 `virtual hosted style`。\n- 如需保留原始 Bucket 字段，请使用 `--output json`。",
	"cmd.object_storages.create.short":     "创建对象存储",
	"cmd.object_storages.create.long":      "按已验证的 `/api/v2/createStorage` 请求形状创建对象存储，或通过 `--file` 直接提交完整 JSON 请求体。",
	"cmd.object_storages.create.examples":  "./hyperbdrctl object-storages create --display-name aliyun-beijing --auth-url oss-cn-beijing.aliyuncs.com --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk> --bucket-mode existing --bucket-name <bucket> --internal-endpoint oss-cn-beijing-internal.aliyuncs.com\n./hyperbdrctl object-storages create --display-name aliyun-beijing --auth-url oss-cn-beijing.aliyuncs.com --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk> --bucket-mode new --bucket-name data-sync-storage-20260527163153-iioq38 --internal-endpoint oss-cn-beijing-internal.aliyuncs.com\n./hyperbdrctl object-storages create --file ./tmp/object-storage-create.json",
	"cmd.object_storages.create.notes":     "- 写操作命令。\n- 提供 `--file` 时，CLI 会把该 JSON 直接提交到 `/api/v2/createStorage`。\n- 不提供 `--file` 时，CLI 默认把 `metadata.cloud_type_select` 组装为 `<cloud-type>,<region-id>`，也可以手工覆盖。",
	"cmd.oss.short":                        "目标对象存储管理",
	"cmd.oss.long":                         "查看、查询目录、创建或删除目标对象存储配置。",
	"cmd.oss.examples":                     "hyperbdrctl oss list\nhyperbdrctl oss catalog\nhyperbdrctl oss create --help\nhyperbdrctl oss delete --id <storage_id> --force",
	"cmd.oss.notes":                        "- `list`、`detail` 和 `catalog` 为只读查询。\n- `catalog` 会读取当前平台的 `/static/json/s3.json`。\n- `buckets` 用于校验凭证并读取 Bucket 列表。\n- `create` 和 `delete` 会发起写操作。\n- `delete` 会先检查关联主机；存在关联时需追加 `--force`。\n- `wait` 用于轮询对象存储创建结果。",
	"cmd.oss.usage_line":                   "hyperbdrctl oss <命令> [参数]",
	"cmd.oss.usage_notes":                  "管理目标对象存储，包括查询云厂商和区域、验证存储桶、创建、删除和等待完成。\n\n建议按下面顺序操作：\n\n1. 先查看当前平台支持的云厂商和区域：\n    hyperbdrctl oss catalog\n    hyperbdrctl oss catalog --provider aliyun\n\n2. 创建前先验证存储桶是否可见：\n    hyperbdrctl oss buckets --help\n\n3. 确认参数后执行创建：\n    hyperbdrctl oss create --help\n\n4. 如需查看已有目标对象存储列表，可以执行：\n    hyperbdrctl oss list\n\n5. 如需删除已有目标对象存储，可以执行：\n    hyperbdrctl oss delete --help\n\n6. 创建成功后，根据返回的存储 ID 查看结果或等待最终状态：\n    hyperbdrctl oss detail --id <storage_id>\n    hyperbdrctl oss wait --id <storage_id>",
	"cmd.oss.list.short":                   "列出目标对象存储",
	"cmd.oss.list.long":                    "查询平台中已创建的目标对象存储。",
	"cmd.oss.list.examples":                "hyperbdrctl oss list --page 1 --page-size 100",
	"cmd.oss.list.notes":                   "- 未识别参数会透传为查询参数。\n- 该命令固定查询 `objectstorage` 类型。",
	"cmd.oss.list.usage_line":              "hyperbdrctl oss list [参数]",
	"cmd.oss.list.usage_notes":             "查询平台中已经创建的目标对象存储列表。\n\n直接查询默认分页结果时，可以执行：\n  hyperbdrctl oss list\n\n如果需要显式指定分页参数，可以执行：\n  hyperbdrctl oss list --page 1 --page-size 100\n\n该命令固定查询 `object` 类型，不需要再传类型参数。\n\n如果需要把结果交给其它工具处理，可以执行：\n  hyperbdrctl --output json oss list\n\n查询到目标对象存储 ID 后，可以继续执行：\n  hyperbdrctl oss detail --id <storage_id>",
	"cmd.oss.detail.short":                 "查看目标对象存储详情",
	"cmd.oss.detail.long":                  "根据 ID 读取单个目标对象存储详情对象。",
	"cmd.oss.detail.examples":              "hyperbdrctl oss detail --id <storage_id>",
	"cmd.oss.detail.notes":                 "- 未识别参数会透传为查询参数。\n- 如需完整嵌套存储元数据，请使用 JSON 输出。",
	"cmd.oss.detail.usage_line":            "hyperbdrctl oss detail [参数]",
	"cmd.oss.detail.usage_notes":           "根据目标对象存储 ID 读取单个目标对象存储详情。\n\n最小查询方式如下：\n  hyperbdrctl oss detail --id <storage_id>\n\n目标对象存储 ID 通常来自：\n  hyperbdrctl oss list\n  hyperbdrctl oss create\n\n如果需要查看原始字段并继续做自动化处理，可以执行：\n  hyperbdrctl --output json oss detail --id <storage_id>",
	"cmd.oss.wait.short":                   "等待目标对象存储创建完成",
	"cmd.oss.wait.long":                    "轮询单个目标对象存储，直到创建结果进入成功、失败或超时。",
	"cmd.oss.wait.examples":                "hyperbdrctl oss wait --id <storage_id>\nhyperbdrctl --output json oss wait --id <storage_id> --interval-seconds 10 --timeout-seconds 900",
	"cmd.oss.wait.notes":                   "- 轮询存储详情接口。\n- 失败或超时时返回非零退出码。\n- 若详情明确显示为非对象存储资源类型，会直接报错退出。",
	"cmd.oss.wait.usage_line":              "hyperbdrctl oss wait [参数]",
	"cmd.oss.wait.usage_notes":             "对象存储创建请求提交后，继续轮询最终状态。\n\n最小等待命令如下：\n  hyperbdrctl oss wait --id <storage_id>\n\n如需调整轮询频率，可附加下面的参数：\n  --interval-seconds <seconds>\n\n如需限制本地等待总时长，可附加下面的参数：\n  --timeout-seconds <seconds>\n\n超时只会停止本地轮询，不会取消后端创建任务。",
	"cmd.oss.buckets.short":                "验证存储桶可见性",
	"cmd.oss.buckets.long":                 "调用已验证的 Bucket 发现接口，确认对象存储凭证和可见 Bucket 列表。",
	"cmd.oss.buckets.examples":             "hyperbdrctl oss buckets --provider aliyun --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk>\nhyperbdrctl oss buckets --auth-url oss-cn-beijing.aliyuncs.com --access-key-id <ak> --access-key-secret <sk> --bucket-lookup dns",
	"cmd.oss.buckets.notes":                "- 只读辅助命令。\n- `bucket-lookup=dns` 对应 UI 的 `virtual hosted style`。\n- 如需保留原始 Bucket 字段，请使用 `--output json`。",
	"cmd.oss.buckets.usage_line":           "hyperbdrctl oss buckets [参数]",
	"cmd.oss.buckets.usage_notes":          "该命令用于验证云端对象存储鉴权信息是否正确，并读取可见的 Bucket 列表。\n\n如需查看 provider 目录模式的帮助，可以执行：\n  hyperbdrctl oss buckets --provider <provider> --help\n\n如需查看 custom 直连模式的帮助，可以执行：\n  hyperbdrctl oss buckets --help\n  hyperbdrctl oss buckets --provider custom --help",
	"cmd.oss.buckets.provider.short":       "按 provider 目录默认值列出 Bucket",
	"cmd.oss.buckets.custom.short":         "验证存储桶可见性",
	"cmd.oss.buckets.provider.usage_notes": "该命令用于使用 provider 目录默认值验证 Bucket 可见性。\n\n建议先查看当前平台下发的目录：\n  hyperbdrctl oss catalog\n  hyperbdrctl oss catalog --provider aliyun\n\n随后按 provider 与 region 查询：\n  hyperbdrctl oss buckets \\\n    --provider aliyun \\\n    --region-id oss-cn-beijing \\\n    --access-key-id <ak> \\\n    --access-key-secret <sk>\n\nCLI 会从 `/static/json/s3.json` 回填 `--auth-url`、`--protocol` 和 `--bucket-lookup`；如果你显式传了这些参数，则命令行值优先。\n\n在创建目标对象存储前，建议先执行该命令确认目标 Bucket 可见。",
	"cmd.oss.buckets.custom.usage_notes":   "创建目标对象存储前，先验证当前凭据是否能访问目标存储桶。\n\n最小查询方式如下：\n  hyperbdrctl oss buckets \\\n    --auth-url 192.168.8.171:9000 \\\n    --access-key-id <ak> \\\n    --access-key-secret <sk>\n\n只有需要显式指定云厂商或区域时，才附加下面的参数：\n  --provider <provider_id>\n  --region-id <region_id>\n\n如需手动覆盖协议或查找方式，可附加下面的参数：\n  --protocol <protocol>\n  --bucket-lookup dns\n\n如需把原始返回结果交给其它工具处理，可附加下面的参数：\n  --output json",
	"cmd.oss.catalog.short":                "查看云厂商和区域",
	"cmd.oss.catalog.long":                 "读取当前平台的 `/static/json/s3.json`，查看支持的对象存储 provider 或单个 provider 下的区域目录。",
	"cmd.oss.catalog.examples":             "hyperbdrctl oss catalog\nhyperbdrctl oss catalog --provider aliyun\nhyperbdrctl --output json oss catalog --provider huaweicloud",
	"cmd.oss.catalog.notes":                "- 只读命令。\n- 直接读取 `/static/json/s3.json`，不走 `/api/v2/*` 包裹响应。\n- 传入 `--provider` 时，JSON 输出返回匹配到的单个 provider 对象。",
	"cmd.oss.catalog.usage_line":           "hyperbdrctl oss catalog [参数]",
	"cmd.oss.catalog.usage_notes":          "查看当前平台支持的云厂商和区域信息。\n\n如需列出全部云厂商，可以执行：\n  hyperbdrctl oss catalog\n\n如需查看单个云厂商下可用的区域，可以执行：\n  hyperbdrctl oss catalog --provider aliyun\n\n如需把原始结果交给其它工具处理，可附加下面的参数：\n  --output json",
	"cmd.oss.create.short":                 "创建目标对象存储",
	"cmd.oss.create.long":                  "按已验证的 `/api/v2/createStorage` 请求形状创建目标对象存储。",
	"cmd.oss.create.examples":              "hyperbdrctl oss create --provider aliyun --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk> --bucket-mode existing --bucket-name <bucket>\nhyperbdrctl oss create --auth-url 192.168.8.171:9000 --access-key-id <ak> --access-key-secret <sk> --bucket-mode new --bucket-name <bucket>",
	"cmd.oss.create.notes":                 "- 写操作命令。\n- 传入 `--provider <provider>` 和 `--region-id` 时，CLI 会先读取 `/static/json/s3.json`，回填默认端点、协议和 bucket 寻址方式。\n- 未传 `--provider`，或显式传 `--provider custom` 时，CLI 走 custom 直连模式。\n- `metadata.cloud_type_select` 已转为内部 CLI 状态，不再作为公开参数暴露。",
	"cmd.oss.create.usage_line":            "hyperbdrctl oss create [参数]",
	"cmd.oss.create.usage_notes":           "该命令用于按已验证的对象存储请求形状创建目标对象存储。\n\n如需查看 provider 目录模式的帮助，可以执行：\n  hyperbdrctl oss create --provider <provider> --help\n\n如需查看 custom 直连模式的帮助，可以执行：\n  hyperbdrctl oss create --help\n  hyperbdrctl oss create --provider custom --help",
	"cmd.oss.create.provider.short":        "按 provider 目录默认值创建对象存储",
	"cmd.oss.create.custom.short":          "创建目标对象存储",
	"cmd.oss.create.provider.usage_notes":  "该命令用于使用 provider 目录默认值创建目标对象存储。\n\n创建前，可以先查看当前平台下发的目录：\n  hyperbdrctl oss catalog\n  hyperbdrctl oss catalog --provider aliyun\n\n传入 `--provider <provider>` 和 `--region-id` 时，CLI 会从 `/static/json/s3.json` 回填 `--auth-url`、`--public-endpoint`、`--internal-endpoint`、`--protocol` 和 `--bucket-lookup`；如果你显式传了这些参数，则命令行值优先。\n\n如果省略 `--display-name`，CLI 会按当前语言使用 provider 名称和 region 名称生成默认显示名称。\n\n最小创建命令如下：\n  hyperbdrctl oss create \\\n    --provider aliyun \\\n    --region-id oss-cn-beijing \\\n    --access-key-id <ak> \\\n    --access-key-secret <sk> \\\n    --bucket-mode existing \\\n    --bucket-name <bucket>\n\n如需只检查最终请求体而不实际创建，可附加：\n  --preview-request\n\n创建成功后，可以继续执行：\n  hyperbdrctl oss wait --id <storage_id>\n  hyperbdrctl oss detail --id <storage_id>",
	"cmd.oss.create.profile_notes":         "当前已从平台目录解析 provider Profile：\n  云厂商：%s（%s）\n\n继续通过 `--region-id` 选择该云厂商下的具体区域；执行创建或带区域查看 help 时会校验区域并解析精确默认值。",
	"cmd.oss.create.region_notes":          "当前已从平台目录解析精确 Profile：\n  云厂商：%s（%s）\n  区域：%s（%s）\n  鉴权地址：%s\n  公网请求域名：%s\n  内网请求域名：%s\n  协议：%s\n  存储桶寻址方式：%s",
	"cmd.oss.create.providers_label":       "云厂商:",
	"cmd.oss.create.custom.usage_notes":    "创建目标对象存储。\n\n默认按自定义对象存储方式创建，不依赖云厂商和区域目录。\n\n最小创建命令如下：\n  hyperbdrctl oss create \\\n    --auth-url 192.168.8.171:9000 \\\n    --access-key-id <ak> \\\n    --access-key-secret <sk> \\\n    --bucket-mode new \\\n    --bucket-name <bucket>\n\n只有需要显式指定云厂商或区域时，才附加下面的参数：\n  --provider <provider_id>\n  --region-id <region_id>\n\n如需先检查最终请求体，可附加下面的参数：\n  --preview-request\n\n创建成功后，建议继续执行：\n  hyperbdrctl oss wait --id <storage_id>\n  hyperbdrctl oss detail --id <storage_id>",
	"cmd.oss.delete.short":                 "删除目标对象存储",
	"cmd.oss.delete.long":                  "删除指定的目标对象存储；删除前会先检查关联资源。",
	"cmd.oss.delete.examples":              "hyperbdrctl oss delete --id <storage_id>\nhyperbdrctl oss delete --id <storage_id> --force",
	"cmd.oss.delete.notes":                 "- 写操作命令。\n- CLI 会先查询 `/api/v2/getStorageAssociatedResources`。\n- 有关联主机时需追加 `--force`。",
	"cmd.oss.delete.usage_line":            "hyperbdrctl oss delete [参数]",
	"cmd.oss.delete.usage_notes":           "删除指定的目标对象存储。\n\n最小删除方式如下：\n  hyperbdrctl oss delete --id <storage_id>\n\nCLI 会先检查该对象存储是否仍有关联主机；如果存在关联，会停止删除并提示追加：\n  --force\n\n只有在确认关联主机可以接受强制删除时，才执行：\n  hyperbdrctl oss delete --id <storage_id> --force\n\n目标对象存储 ID 通常来自：\n  hyperbdrctl oss list\n  hyperbdrctl oss detail --id <storage_id>",
	"cmd.target.oss.short":                 "目标对象存储配置",
	"cmd.target.oss.long":                  "查看、查询目录、创建或删除目标对象存储配置。",
	"cmd.target.oss.examples":              "hyperbdrctl target oss list\nhyperbdrctl target oss catalog\nhyperbdrctl target oss create --help\nhyperbdrctl target oss delete --id <storage_id> --force",
	"cmd.target.oss.notes":                 "- `list`、`detail` 和 `catalog` 为只读查询。\n- `catalog` 会读取当前平台的 `/static/json/s3.json`。\n- `buckets` 用于校验凭证并读取 Bucket 列表。\n- `create` 和 `delete` 会发起写操作。\n- `delete` 会先检查关联主机；存在关联时需追加 `--force`。",
	"cmd.target.oss.usage_line":            "hyperbdrctl target oss <命令> [参数]",
	"cmd.target.oss.usage_notes":           "该命令组用于目标对象存储的查询、provider 目录查询、Bucket 探测、创建、删除和等待操作。\n\n如果需要查看当前已存在的目标对象存储列表，可以执行：\n  hyperbdrctl target oss list\n\n如果需要查看当前平台下发的对象存储目录，可以执行：\n  hyperbdrctl target oss catalog\n  hyperbdrctl target oss catalog --provider aliyun\n\n如果需要先验证云端 Bucket 是否可见，可以执行：\n  hyperbdrctl target oss buckets --help\n\n如果需要创建新的目标对象存储，可以执行：\n  hyperbdrctl target oss create --help\n\n如果需要删除已有目标对象存储，可以执行：\n  hyperbdrctl target oss delete --help\n\n创建返回存储 ID 后，如需等待最终状态，可以执行：\n  hyperbdrctl target oss wait --id <storage_id>",
	"cmd.target.oss.list.short":            "列出目标对象存储",
	"cmd.target.oss.list.long":             "查询平台中已创建的目标对象存储，默认 type=objectstorage。",
	"cmd.target.oss.list.examples":         "hyperbdrctl target oss list --page 1 --page-size 100",
	"cmd.target.oss.list.notes":            "- 未识别参数会透传为查询参数。\n- 默认只查询 `objectstorage` 类型。",
	"cmd.target.oss.list.usage_line":       "hyperbdrctl target oss list [参数]",
	"cmd.target.oss.list.usage_notes":      "该命令用于查询平台中已经创建的目标对象存储列表。\n\n直接查询默认分页结果时，可以执行：\n  hyperbdrctl target oss list\n\n如果需要显式指定分页参数，可以执行：\n  hyperbdrctl target oss list --page 1 --page-size 100\n\n该命令默认只查询 `objectstorage` 类型。\n\n如果需要把结果交给其它工具处理，可以执行：\n  hyperbdrctl --output json target oss list\n\n查询到目标对象存储 ID 后，可以继续执行：\n  hyperbdrctl target oss detail --id <storage_id>",
	"cmd.target.oss.detail.short":          "查看目标对象存储详情",
	"cmd.target.oss.detail.long":           "根据 ID 读取单个目标对象存储详情对象。",
	"cmd.target.oss.detail.examples":       "hyperbdrctl target oss detail --id <storage_id>",
	"cmd.target.oss.detail.notes":          "- 未识别参数会透传为查询参数。\n- 如需完整嵌套存储元数据，请使用 JSON 输出。",
	"cmd.target.oss.wait.short":            "等待目标对象存储创建完成",
	"cmd.target.oss.wait.long":             "轮询单个目标对象存储，直到创建结果进入成功、失败或超时。",
	"cmd.target.oss.wait.examples":         "hyperbdrctl target oss wait --id <storage_id>\nhyperbdrctl --output json target oss wait --id <storage_id> --interval-seconds 10 --timeout-seconds 900",
	"cmd.target.oss.wait.notes":            "- 轮询存储详情接口。\n- 失败或超时时返回非零退出码。\n- 若详情明确显示为非对象存储资源类型，会直接报错退出。",
	"cmd.target.oss.detail.usage_line":     "hyperbdrctl target oss detail [参数]",
	"cmd.target.oss.detail.usage_notes":    "该命令用于根据对象存储 ID 读取单个目标对象存储详情。\n\n最小查询方式如下：\n  hyperbdrctl target oss detail --id <storage_id>\n\n`storage_id` 通常来自下面这些命令的输出：\n  hyperbdrctl target oss list\n  hyperbdrctl target oss create\n\n如果需要查看原始字段并继续做自动化处理，可以执行：\n  hyperbdrctl --output json target oss detail --id <storage_id>",
	"cmd.target.oss.wait.usage_line":       "hyperbdrctl target oss wait [参数]",
	"cmd.target.oss.wait.usage_notes":      "该命令用于在对象存储创建请求已经提交后，继续轮询最终状态。\n\n最小等待命令如下：\n  hyperbdrctl target oss wait --id <storage_id>\n\n如需调整轮询频率，可附加：\n  --interval-seconds <seconds>\n\n如需限制本地等待总时长，可附加：\n  --timeout-seconds <seconds>\n\n超时只会停止本地轮询，不会取消后端创建任务。",
	"cmd.target.oss.buckets.short":         "列出云端对象存储 Bucket",
	"cmd.target.oss.buckets.long":          "调用已验证的 Bucket 发现接口，确认对象存储凭证和可见 Bucket 列表。",
	"cmd.target.oss.buckets.examples":      "hyperbdrctl target oss buckets --provider aliyun --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk>\nhyperbdrctl target oss buckets --auth-url oss-cn-beijing.aliyuncs.com --access-key-id <ak> --access-key-secret <sk> --bucket-lookup dns",
	"cmd.target.oss.buckets.notes":         "- 只读辅助命令。\n- `bucket-lookup=dns` 对应 UI 的 `virtual hosted style`。\n- 如需保留原始 Bucket 字段，请使用 `--output json`。",
	"cmd.target.oss.buckets.usage_line":    "hyperbdrctl target oss buckets [参数]",
	"cmd.target.oss.buckets.usage_notes":   "该命令用于验证云端对象存储鉴权信息是否正确，并读取可见的 Bucket 列表。\n\n如需使用 provider 目录默认值查询，可以执行：\n  hyperbdrctl target oss buckets \\\n    --provider aliyun \\\n    --region-id oss-cn-beijing \\\n    --access-key-id <ak> \\\n    --access-key-secret <sk>\n\n此时 CLI 会从 `/static/json/s3.json` 回填 `--auth-url`、`--protocol` 和 `--bucket-lookup`；如果你显式传了这些参数，则命令行值优先。\n\n如需改用显式 `auth-url` 方式查询，可以执行：\n  hyperbdrctl target oss buckets \\\n    --auth-url oss-cn-beijing.aliyuncs.com \\\n    --access-key-id <ak> \\\n    --access-key-secret <sk>\n\n如需调整 Bucket 查找方式，可以附加：\n  --bucket-lookup dns\n\n在创建目标对象存储前，建议先执行该命令确认目标 Bucket 可见。",
	"cmd.target.oss.catalog.short":         "查看对象存储 provider 目录",
	"cmd.target.oss.catalog.long":          "读取当前平台的 `/static/json/s3.json`，查看支持的对象存储 provider 或单个 provider 下的区域目录。",
	"cmd.target.oss.catalog.examples":      "hyperbdrctl target oss catalog\nhyperbdrctl target oss catalog --provider aliyun\nhyperbdrctl --output json target oss catalog --provider huaweicloud",
	"cmd.target.oss.catalog.notes":         "- 只读命令。\n- 直接读取 `/static/json/s3.json`，不走 `/api/v2/*` 包裹响应。\n- 传入 `--provider` 时，JSON 输出返回匹配到的单个 provider 对象。",
	"cmd.target.oss.catalog.usage_line":    "hyperbdrctl target oss catalog [参数]",
	"cmd.target.oss.catalog.usage_notes":   "该命令用于查看当前平台发布的对象存储 provider 与 region 目录。\n\n如需列出全部支持的 provider，可以执行：\n  hyperbdrctl target oss catalog\n\n如需查看单个 provider 下的 region 默认值，可以执行：\n  hyperbdrctl target oss catalog --provider aliyun\n\n如需把原始目录对象交给其它工具处理，可附加：\n  --output json",
	"cmd.target.oss.create.short":          "创建目标对象存储",
	"cmd.target.oss.create.long":           "按已验证的 `/api/v2/createStorage` 请求形状创建目标对象存储。",
	"cmd.target.oss.create.examples":       "hyperbdrctl target oss create --provider aliyun --region-id oss-cn-beijing --access-key-id <ak> --access-key-secret <sk> --bucket-mode existing --bucket-name <bucket>\nhyperbdrctl target oss create --auth-url 192.168.8.171:9000 --access-key-id <ak> --access-key-secret <sk> --bucket-mode new --bucket-name <bucket>",
	"cmd.target.oss.create.notes":          "- 写操作命令。\n- 传入 `--provider <provider>` 和 `--region-id` 时，CLI 会先读取 `/static/json/s3.json`，回填默认端点、协议和 bucket 寻址方式。\n- 未传 `--provider`，或显式传 `--provider custom` 时，CLI 走 custom 直连模式，并把 `cloud_type` 和 `metadata.cloud_type_select` 默认设为 `custom`。\n- 如有需要，仍可手动覆盖 `metadata.cloud_type_select`。",
	"cmd.target.oss.create.usage_line":     "hyperbdrctl target oss create [参数]",
	"cmd.target.oss.create.usage_notes":    "该命令用于按已验证的对象存储请求形状创建目标对象存储。\n\n创建前，可以先查看当前平台下发的目录：\n  hyperbdrctl target oss catalog\n  hyperbdrctl target oss catalog --provider aliyun\n\ncatalog provider 模式：\n  传入 `--provider <provider>` 和 `--region-id` 时，CLI 会从 `/static/json/s3.json` 回填 `--auth-url`、`--public-endpoint`、`--internal-endpoint`、`--protocol` 和 `--bucket-lookup`；如果你显式传了这些参数，则命令行值优先。\n  如果省略 `--display-name`，CLI 会按当前语言使用 provider 名称和 region 名称生成默认显示名称。\n\ncustom 直连模式：\n  未传 `--provider`，或显式传 `--provider custom` 时，CLI 会按 `custom` 创建对象存储。\n  此模式下 `--auth-url` 为必填，`--region-id` 可选，`metadata.cloud_type_select` 默认固定为 `custom`，默认显示名称为 `其它平台`。\n\n使用目录默认值并绑定已有 Bucket 时，可以执行：\n  hyperbdrctl target oss create \\\n    --provider aliyun \\\n    --region-id oss-cn-beijing \\\n    --access-key-id <ak> \\\n    --access-key-secret <sk> \\\n    --bucket-mode existing \\\n    --bucket-name <bucket>\n\n如需创建 custom 直连对象存储，可以执行：\n  hyperbdrctl target oss create \\\n    --auth-url 192.168.8.171:9000 \\\n    --access-key-id <ak> \\\n    --access-key-secret <sk> \\\n    --bucket-mode new \\\n    --bucket-name <bucket>\n\n如需只检查最终请求体而不实际创建，可以附加：\n  --preview-request\n\n创建成功后，可以继续执行：\n  hyperbdrctl target oss wait --id <storage_id>\n  hyperbdrctl target oss detail --id <storage_id>",
	"cmd.target.oss.delete.short":          "删除目标对象存储",
	"cmd.target.oss.delete.long":           "删除指定的目标对象存储；删除前会先检查关联资源。",
	"cmd.target.oss.delete.examples":       "hyperbdrctl target oss delete --id <storage_id>\nhyperbdrctl target oss delete --id <storage_id> --force",
	"cmd.target.oss.delete.notes":          "- 写操作命令。\n- CLI 会先查询 `/api/v2/getStorageAssociatedResources`。\n- 有关联主机时需追加 `--force`。",
	"cmd.target.oss.delete.usage_line":     "hyperbdrctl target oss delete [参数]",
	"cmd.target.oss.delete.usage_notes":    "该命令用于删除指定的目标对象存储。\n\n最小删除方式如下：\n  hyperbdrctl target oss delete --id <storage_id>\n\nCLI 会先检查该对象存储是否仍有关联主机；如果存在关联，会停止删除并提示追加：\n  --force\n\n只有在确认关联主机可以接受强制删除时，才执行：\n  hyperbdrctl target oss delete --id <storage_id> --force\n\n`storage_id` 通常来自下面这些命令的输出：\n  hyperbdrctl target oss list\n  hyperbdrctl target oss detail --id <storage_id>",
}
