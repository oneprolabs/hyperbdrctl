package i18n

var configZH = map[string]string{
	"config.saved":                         "配置已保存",
	"cmd.config.short":                     "查看或更新 CLI 默认配置",
	"cmd.config.long":                      "查看或更新当前机器上保存的地址、凭证、语言、场景、输出格式和 TLS 偏好。",
	"cmd.config.examples":                  "./hyperbdrctl config get\n./hyperbdrctl config set \\\n  --host https://example:10443 \\\n  --username admin \\\n  --password secret",
	"cmd.config.usage_line":                "hyperbdrctl config <命令> [参数]",
	"cmd.config.usage_notes":               "查看当前机器上已保存的地址、凭证、语言、场景和输出格式：\n  hyperbdrctl config get\n\n初始化或更新本地默认配置：\n  hyperbdrctl config set \\\n    --host https://example:10443 \\\n    --username <username> \\\n    --password <password>\n\n如果不希望把地址或凭证写入本地配置，也可以按当前 Shell 的方式设置环境变量。\n\n完整参数、环境变量和校验规则请继续查看：\n  hyperbdrctl config get --help\n  hyperbdrctl config set --help\n\n配置读取优先级如下：\n  命令行参数 > 环境变量 > 配置文件 > 默认值\n\n完成配置后，可以执行下面的命令验证后续命令是否会使用新的默认值：\n  hyperbdrctl config get\n  hyperbdrctl host list",
	"cmd.config.notes":                     "- 生效优先级固定为命令行参数 > 环境变量 > 配置文件 > 默认值。\n- `config get` 默认会隐藏已保存密码，除非显式指定 `--show-secret`。",
	"cmd.config.workflow":                  "1. 先运行 `config get` 查看当前保存值和本地路径。\n2. 需要调整地址、凭证或本地默认值时，再运行 `config set`。\n3. 保存后重新执行 `config get`，确认后续命令会使用新的默认值。",
	"cmd.config.related":                   "./hyperbdrctl config get\n./hyperbdrctl config set --help",
	"cmd.config.next_steps":                "./hyperbdrctl host list\n./hyperbdrctl boot-config get --id <host_id>",
	"cmd.config.get.short":                 "显示已保存的本地配置",
	"cmd.config.get.long":                  "读取已保存的本地配置，以及配置文件和 token 缓存路径。除非显式指定 `--show-secret`，否则密码会保持隐藏。",
	"cmd.config.get.examples":              "./hyperbdrctl config get\n./hyperbdrctl config get --show-secret",
	"cmd.config.get.usage_line":            "hyperbdrctl config get [参数]",
	"cmd.config.get.usage_notes":           "读取当前机器上已保存的本地配置，并显示配置文件路径和 token 缓存路径。\n\n查看已保存配置：\n  hyperbdrctl config get\n\n默认情况下，输出中的已保存密码会保持隐藏。\n\n如需明文查看已保存密码：\n  hyperbdrctl config get --show-secret\n\n该命令只读取本地文件，不会调用远端 API。\n\n把结果交给其他工具处理：\n  hyperbdrctl --output json config get\n\n如果查看后需要更新默认配置，可以继续执行：\n  hyperbdrctl config set \\\n    --host https://example:10443 \\\n    --username <username> \\\n    --password <password>",
	"cmd.config.get.notes":                 "- 该命令只读取本地文件，不会调用远端 API。\n- 需要给其他工具消费原始字段名时，可使用 `--output json`。",
	"cmd.config.get.related":               "./hyperbdrctl config set --help",
	"cmd.config.get.next_steps":            "./hyperbdrctl config set \\\n  --host https://example:10443 \\\n  --username admin \\\n  --password secret",
	"cmd.config.set.short":                 "校验并保存本地 CLI 配置",
	"cmd.config.set.long":                  "把当前命令行传入的值与已保存配置合并后，用最终凭证先登录校验，再写入规范化配置和 token 缓存。",
	"cmd.config.set.examples":              "# 首次登录并保存配置\n./hyperbdrctl config set \\\n  --host https://example:10443 \\\n  --username admin \\\n  --password secret\n\n# 基于已保存凭证更新默认场景和输出格式\n./hyperbdrctl config set \\\n  --scene migration \\\n  --lang zh_cn \\\n  --output json",
	"cmd.config.set.usage_line":            "hyperbdrctl config set [参数]",
	"cmd.config.set.usage_notes":           "执行前需要准备目标环境的平台地址、登录用户名和密码。\n\n首次初始化本地配置：\n  hyperbdrctl config set \\\n    --host https://example:10443 \\\n    --username <username> \\\n    --password <password>\n\n如果 host、username 和 password 已保存，可以只更新默认场景、语言或输出格式：\n  hyperbdrctl config set \\\n    --scene migration \\\n    --lang zh_cn \\\n    --output json\n\n如果命令行省略某个字段，CLI 会按下面的优先级补齐：\n  命令行参数 > 环境变量 > 配置文件 > 默认值\n\n支持通过环境变量提供同类配置项，常用环境变量包括：\n  HYPERBDR_HOST\n  HYPERBDR_USERNAME\n  HYPERBDR_PASSWORD\n  HYPERBDR_SCENE\n  HYPERBDR_LANG\n  HYPERBDR_OUTPUT\n  HYPERBDR_INSECURE\n  HYPERBDR_DEBUG\n\n以 Bash 为例，可以先设置环境变量，再执行保存：\n  export HYPERBDR_HOST=https://example:10443\n  export HYPERBDR_USERNAME=<username>\n  export HYPERBDR_PASSWORD=<password>\n  hyperbdrctl config set\n\n保存前会先执行登录校验；如果登录失败，不会保存新的配置。\n\n如需确认保存结果，可以继续执行：\n  hyperbdrctl config get\n\n保存成功后，可以执行下面的命令验证后续命令是否会使用新的默认值：\n  hyperbdrctl host list",
	"cmd.config.set.notes":                 "- 通过当前命令的本地参数传入地址、凭证、场景和 TLS 行为。\n- 登录失败时不会保存新配置。\n- `--insecure` 只应用于明确允许跳过 TLS 证书校验的测试环境。",
	"cmd.config.set.minimum_flags":         "初始化新的本地配置时，至少传入：\n  --host\n  --username\n  --password",
	"cmd.config.set.automatic_behavior":    "- 如果当前命令行省略某个字段，`config set` 会先按环境变量、已保存配置和默认值依次补齐，再执行登录校验。\n- 如果 host、username、password 已经保存，可以不重复传凭证，只更新 scene、lang 或 output。\n- CLI 会先用合并后的候选配置完成登录，再写入新的配置文件和 token 缓存。",
	"cmd.config.set.workflow":              "1. 先准备目标环境的平台地址、登录用户名和密码。\n2. 在同一条 `config set` 命令里传入必需的本地参数。\n3. 保存后重新执行 `config get`，确认默认值和掩码后的密码输出。",
	"cmd.config.set.common_optional_flags": "- `--scene`：持久化默认平台场景（`dr` 或 `migration`）。\n- `--lang`：持久化默认 CLI 语言和 `X-LANG` 请求头。\n- `--output`：持久化默认输出模式（`table` 或 `json`）。\n- `--insecure`：仅在明确的测试环境里跳过 TLS 证书校验。\n- `--debug`：向 stderr 输出已脱敏的 HTTP 调试日志。",
	"cmd.config.set.related":               "./hyperbdrctl config get\n./hyperbdrctl config get --show-secret",
	"cmd.config.set.next_steps":            "./hyperbdrctl host list\n./hyperbdrctl boot-config get --id <host_id>",
}
